{
  "name": "wirestrike",
  "version": "1.0.0",
  "description": "WireStrike — Protocol Security Testing Suite for TLS, HTTP/2, QUIC & TCP",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "cli": "node cli.js",
    "server": "node server.js",
    "client": "node client.js",
    "agent-client": "node client.js --agent",
    "agent-server": "node server.js --agent"
  },
  "dependencies": {
    "@currentspace/http3": "^0.4.0",
    "xml2js": "^0.6.2"
  },
  "optionalDependencies": {
    "raw-socket": "^1.7.0"
  },
  "devDependencies": {
    "electron": "^40.6.0"
  }
}
